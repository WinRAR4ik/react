import { useEffect, useState } from "react";

export function Home() {


  return (
    <div>
      <h2>Home</h2>
      {/*<button onClick={() => console.log(postsWithImages)}>CLick</button>*/}
      {/*{postsWithImages.map((post, index) => (*/}
      {/*  <div key={post.objectID}>*/}
      {/*    <h3>{post.title}</h3>*/}
      {/*    <img src={post.primaryImage} alt={post.title} style={{maxWidth: '200px'}} />*/}
      {/*  </div>*/}
      {/*))}*/}
    </div>
  );
}